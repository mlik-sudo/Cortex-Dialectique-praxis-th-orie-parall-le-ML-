# ⚡ /scripts/ — Domaine de @Codex-Engineer

> "It compiles, ship it."

Ce dossier contient la **praxis exécutable** du protocole : scripts de boot, checks, et automatisations locales.

- `boot-sequence.sh` : checklist de démarrage (standup asynchrone)
- `doctor.sh` : sanity-check de la structure du dépôt
