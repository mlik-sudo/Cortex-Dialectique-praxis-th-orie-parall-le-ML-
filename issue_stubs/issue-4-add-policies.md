**Title:** feat(policies): add routing.yaml + budgets.yaml (v0)

**Body:**

Create the initial version of the `routing.yaml` and `budgets.yaml` files to define the policies for the multi-agent orchestrator.
