# 📜 /policies/ — Control Plane (théorie)

Ce dossier est la **source canonique** pour les règles de gouvernance, routing et budgets.

- `routing.yaml` : préférences de drivers par skill
- `budgets.yaml` : enveloppes (RPM/RPD/CPU)
- `limits.yaml` : limites de concurrence, timeouts, retries

Notes:
- `project-space/` contient la *praxis* (benchmarks, dashboards, résultats), pas les règles.
